/** @type {import('next').NextConfig} */
const nextConfig = {
  // API calls are made from the browser to Render backend
  // No need for rewrites since we use absolute NEXT_PUBLIC_API_URL
};

module.exports = nextConfig;
